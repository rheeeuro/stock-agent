"""
AI 프롬프트 템플릿 모듈 - YouTube/Telegram 공통 분석 프롬프트 관리
"""

# 공통 분석 지시사항 (YouTube, Telegram 모두 사용)
_COMMON_ANALYSIS_INSTRUCTIONS = """
[중요 지시사항]
먼저 이 메시지가 **'주식, 경제, 투자, 기업 분석, 시장 전망'**과 관련된 내용인지 판단해.

1. 만약 **관련 없는 내용(일상, 먹방, 게임, 단순 유머 등)**이라면:
   반드시 JSON의 sentiment_score를 **-1**로 설정하고 content는 비워둬.
   
2. **관련 있는 내용**이라면 다음 필드를 분석해서 반드시 **순수 JSON**으로만 출력해.
    - sentiment_score: 시장 전망 점수 (0: 폭락/공포 ~ 50: 중립 ~ 100: 폭등/탐욕). 관련 없으면 -1
    - content: 아래 Markdown 템플릿을 반드시 따르는 분석 리포트 문자열. 관련 없으면 빈 문자열 ""
    - related_companies: 텍스트에서 언급된 상장 기업명 리스트 (최대 5개). 없으면 빈 리스트 []
        🚨주의: 반드시 '현재 주식 시장에 상장된 공식 기업'의 이름만 추출해라. Grok, OpenAI, ChatGPT 같은 제품명, AI 모델, 비상장 기업은 절대 포함하지 마라!
        🚨주의: 기업명만 단독으로 작성해라. 절대로 "엔비디아(NVDA)", "Tesla(TSLA)" 같은 '이름(티커)' 형식을 사용하지 마라! 올바른 예: "엔비디아", "테슬라". 잘못된 예: "엔비디아(NVDA)", "Tesla(TSLA)", "삼성전자(005930)"
        🚨주의: 티커 심볼(NVDA, AAPL, TSLA, 005930 등)을 절대 넣지 마라! 반드시 정식 기업명(엔비디아, 애플, 테슬라, 삼성전자)으로 작성해라.
        🚨주의: "반도체주", "AI주", "2차전지주", "배터리주", "자동차주", "IT주", "바이오주" 같은 섹터/테마/업종 카테고리명은 기업이 아니다. 절대 포함하지 마라! 구체적인 개별 기업명만 넣어라.
    - market: 주로 다루는 시장 ("US", "KR", "CRYPTO", "UNKNOWN" 중 하나)

    [content Markdown 템플릿 - 반드시 이 구조를 따를 것]:
    
        ## 1. 3줄 핵심 요약
        - (요약 1)
        - (요약 2)  
        - (요약 3)
        
        ## 2. 주요 언급 종목
        - **종목명**: (호재/악재 판단)
        
        ## 3. 대응 전략
        > (한 줄 조언)

🚨 환각(Hallucination) 방지 절대 규칙:
- 반드시 **제공된 텍스트에 명시적으로 작성된 내용만** 분석해라. 텍스트에 없는 정보를 절대 추측하거나 지어내지 마라.
- URL만 있고 실제 기사/분석 내용이 없는 텍스트는 분석할 수 없다. sentiment_score를 **-1**로 설정해라.
- sentiment_score는 반드시 텍스트의 실제 논조와 일치해야 한다:
    - "급락", "하락", "폭락", "우려", "경고", "리스크" 등 부정적 키워드가 포함되면 점수는 반드시 50 미만이어야 한다.
    - "급등", "상승", "호조", "기대감", "호재" 등 긍정적 키워드가 포함되면 점수는 50 이상이어야 한다.
- related_companies에는 텍스트에서 **직접 언급된 기업만** 포함해라. 유추하거나 연관 기업을 임의로 추가하지 마라.
- title과 content 요약은 반드시 원문 텍스트의 핵심 내용을 반영해야 한다. 원문과 반대되는 방향의 요약은 절대 금지한다.

🚨 출력 형식 절대 규칙:
- 반드시 순수 JSON만 출력해라. 주석(//, /* */)은 절대 사용하지 마라.
- JSON 외의 텍스트, 설명, 인사말은 절대 출력하지 마라.
- content 값은 반드시 위 Markdown 템플릿 형식의 단일 문자열이어야 한다.
"""

_COMMON_JSON_FORMAT = """
[필수 출력 형식 - 순수 JSON만 출력할 것, 주석 금지]:
{{
    "sentiment_score": 75,
    "content": "## 1. 3줄 핵심 요약\\n- 요약1\\n- 요약2\\n- 요약3\\n\\n## 2. 주요 언급 종목\\n- **종목명**: 호재/악재 판단\\n\\n## 3. 대응 전략\\n> 한 줄 조언",
    "related_companies": ["삼성전자", "SK하이닉스"],
    "market": "US"
}}
"""

# YouTube 분석용 프롬프트 (title + content 플레이스홀더)
YOUTUBE_ANALYSIS_PROMPT = """
        영상 제목: {title}
""" + _COMMON_ANALYSIS_INSTRUCTIONS + _COMMON_JSON_FORMAT + """
        [자막 내용]: {content}
"""

# Telegram 분석용 프롬프트 (title 자동 생성 포함)
TELEGRAM_ANALYSIS_PROMPT = _COMMON_ANALYSIS_INSTRUCTIONS + """
    - title: 메시지의 핵심 내용을 한눈에 파악할 수 있는 제목 (🚨 아래 title 작성 규칙 반드시 준수)

    [🚨 title 작성 규칙 - 절대 빈 문자열 금지]:
    - title은 **필수 항목**이다. 절대로 빈 문자열("")이나 "제목", "없음" 같은 의미 없는 값을 넣지 마라.
    - 반드시 메시지의 **핵심 주제, 종목명, 시장 방향성**을 포함한 구체적인 제목을 작성해라.
    - 15~40자 사이의 한국어 제목을 작성해라.
    - 뉴스 헤드라인처럼 간결하고 핵심을 담아라.
    - 좋은 예시: "엔비디아 실적 발표 후 시간외 급등", "미국 CPI 예상 하회로 금리 인하 기대감 확대", "테슬라 중국 판매량 급감 우려"
    - 나쁜 예시: "", "제목", "분석", "텔레그램 메시지", "주식 관련"
""" + """
[필수 출력 형식 - 순수 JSON만 출력할 것, 주석 금지]:
{{
    "sentiment_score": 75,
    "title": "엔비디아 실적 호조에 AI 반도체주 동반 상승 전망",
    "content": "## 1. 3줄 핵심 요약\\n- 요약1\\n- 요약2\\n- 요약3\\n\\n## 2. 주요 언급 종목\\n- **종목명**: 호재/악재 판단\\n\\n## 3. 대응 전략\\n> 한 줄 조언",
    "related_companies": ["삼성전자", "SK하이닉스"],
    "market": "US"
}}

[메시지 내용]: {text}
"""

# 일일 요약 리포트 프롬프트
DAILY_DIGEST_PROMPT = """
[오늘의 리포트 데이터 시작]
{reports_text}
[오늘의 리포트 데이터 끝]

-----------------------
[시스템 절대 지시사항]
너는 위의 데이터를 분석해서 오직 JSON 형식으로만 응답하는 기계다.
영어는 절대 사용하지 말고 무조건 '한국어(Korean)'로만 작성해라.
인사말, 요약, 설명 등은 절대 금지한다.

[필수 출력 형식 - 그대로 복사해서 내용만 채울 것]:
```json
{{
    "buy_stock": "가장 추천하는 매수 종목명 1개",
    "buy_ticker": "매수 종목의 티커 심볼 (예: TSLA, NVDA, 005930.KS 등. 모르면 빈칸)",
    "buy_reason": "매수 추천 이유 1줄 요약",
    "sell_stock": "매도 또는 관망 종목명 1개",
    "sell_ticker": "매도 종목의 티커 심볼 (모르면 빈칸)",
    "sell_reason": "매도/관망 이유 1줄 요약"
}}
```
"""
